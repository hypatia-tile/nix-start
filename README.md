# nix-start

The starting point for Nix-based projects and fundamentsl configuration for NixOS systems.
I test this setup on macOS.

# TODO

We need to install `direnv` and `compdef` for the nix-shell to work properly.

```
bash: compdef: command not found
bash: direnv: command not found
```
